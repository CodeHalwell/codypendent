//! The tool layer (STEP 1.7).
//!
//! Populated by STEP 1.7: `workspace.read_file`, `workspace.search`,
//! `shell.run`, and `git.diff`/`git.apply_patch`, each declaring the
//! capabilities it requires, with structured command execution, timeouts,
//! output caps spilling to artifacts, and observation compaction.
