//! Model providers (STEP 1.9).
//!
//! Populated by STEP 1.9: provider config from `models.toml`, framework client
//! construction via `agent-framework-openai`, and a Phase 1 `ModelPolicy`
//! candidate list with connection-failure fallback.
