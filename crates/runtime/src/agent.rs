//! The framework agent loop (STEP 1.10).
//!
//! Populated by STEP 1.10: the `FrameworkAgentRuntime` adapter driving the
//! Inspect → Plan → Modify → Test → Review → Present loop around an
//! `agent-framework-rs` agent, with policy/approval middleware, artifact and
//! event translation, modes, change sets, and chronicle.
