//! Startup recovery and the failure matrix (STEP 1.14).
//!
//! Populated by STEP 1.14: live runs move to `Recovering`, pending effects are
//! reconciled, and unrecoverable runs are cleanly marked `Failed`.
